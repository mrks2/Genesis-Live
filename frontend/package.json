{
  "name": "@genesis-live/frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Rendu Pixi.js de Genesis Live : planète, entités, HUD, audio.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist .vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@genesis-live/shared": "*",
    "pixi.js": "^7.4.3"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
