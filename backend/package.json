{
  "name": "@genesis-live/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Moteur de simulation Genesis Live : tick loop, règles, persistance, chat adapters.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --build",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Tests à ajouter en Phase 2\" && exit 0"
  },
  "dependencies": {
    "@genesis-live/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
